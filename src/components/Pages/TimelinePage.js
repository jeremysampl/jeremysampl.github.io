import React, { useEffect } from 'react';
import '../../styles/timeline.css';

export default function TimelinePage() {
    useEffect(() => {
        window.scrollTo(0, 0);
    }, []);
    
    return (
        <>

        </>
    );
}